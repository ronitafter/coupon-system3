import { ReactNode } from "react";

const CouponModel ={
     id?: number,
     companyId?: number,
     company?: number,
     title?: string,
     amount?: number,
     price?: number,
     description?: string,
     startDate?: Date,
     endDate?: Date,
     type?: string,
     image?: string,
    name: ReactNode,
}

export default CouponModel;